# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
venv/
env/
.venv/

# Environment variables — never commit secrets
.env

# Trained model artifacts — committed so Render can serve them without retraining
# backend/models/*.pkl   <-- intentionally NOT ignored, needed for deployment
# backend/models/*.json  <-- intentionally NOT ignored, needed for deployment

# Jupyter checkpoints
.ipynb_checkpoints/
notebooks/.ipynb_checkpoints/

# Raw dataset — too large for git, download separately
data/raw/*.csv

# Node.js dependencies
frontend/node_modules/
node_modules/

# Processed data
data/processed/

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
